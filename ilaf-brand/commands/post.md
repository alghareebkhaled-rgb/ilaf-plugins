---
description: Design an ILAF Instagram feed post (1080×1350) in both brand colorways
argument-hint: [topic, e.g. "motor insurance renewal" or "medical cover promo"]
---

Design an ILAF feed post about: $ARGUMENTS

Use the `ilaf-social-design` skill. Follow its full workflow — clarify only what the
request leaves open, show the design plan for approval before building, then execute.

Remember the standing agreement: every 1080×1350 feed post ships in **both** colorways
(slate navy `#333653` and magenta `#A50B7F`) — build both, don't ask which.
